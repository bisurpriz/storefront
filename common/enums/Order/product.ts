export enum CustomizableAreaType {
  TEXT = "special_text",
  IMAGE = "special_image",
}

export enum OrderItemStatus {
  Created,
  Processing,
  Shipped,
  Delivered,
  Canceled,
  Completed,
  Failed,
  Refunded
}
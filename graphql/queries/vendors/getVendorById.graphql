query getVendorById($id: uuid!) {
  tenant_by_pk(id: $id) {
    id
    name
    logo
    legal_company_title
    created_at
    owner {
      products_aggregate {
        aggregate {
          count
        }
      }
      reviews_aggregate {
        aggregate {
          count
        }
      }
    }
  }
}

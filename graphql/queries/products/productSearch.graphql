query searchProducts($search: String) {
  product(
    where: {
      _and: [
        {
          _or: [
            { name: { _ilike: $search } }
            { name: { _similar: $search } }
            {
              product_categories: { category: { name: { _similar: $search } } }
            }
          ]
        }
        { is_active: { _eq: true } }
      ]
    }
  ) {
    name
  }
}

import Spinner from '../Spinner';

const Loading = () => {
  return (
    <section className="py-24">
      <div className="container">
        <Spinner />
      </div>
    </section>
  );
};

export default Loading;
